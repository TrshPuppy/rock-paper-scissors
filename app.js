// commit: Initialize player and computer scores.

const game = () => 
{
    let playerScore = 0; // may have to change to '-1' depending on how choices are scored later.
    let computerScore = 0;


}